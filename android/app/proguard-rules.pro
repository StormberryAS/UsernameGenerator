# The app has no reflection, no serialisation and no JNI, so the defaults from
# proguard-android-optimize.txt cover everything. Kept as a file because AGP
# expects it and because any future rule belongs here rather than in the build script.
-dontobfuscate
